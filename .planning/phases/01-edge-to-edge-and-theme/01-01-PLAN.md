---
phase: 01-edge-to-edge-and-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resume-tailor-android/app/src/main/res/values/themes.xml
  - resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt
  - resume-tailor-android/app/build.gradle.kts
autonomous: true
requirements: [E2E-01, E2E-02]

must_haves:
  truths:
    - "App displays correctly edge-to-edge on Android 15 (API 35)"
    - "No deprecated edge-to-edge warnings in Play Console after rebuild"
    - "Status bar and navigation bar content remains readable on both light and dark backgrounds"
    - "All screen content respects system bar insets — no overlap with status bar or nav bar"
  artifacts:
    - path: "resume-tailor-android/app/src/main/res/values/themes.xml"
      provides: "Updated theme without deprecated statusBarColor/navigationBarColor"
      contains: "Theme.CraftCV"
    - path: "resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt"
      provides: "Proper enableEdgeToEdge() with backward compat"
  key_links:
    - from: "themes.xml"
      to: "AndroidManifest.xml"
      via: "android:theme attribute"
      pattern: "Theme.CraftCV"
    - from: "MainActivity.kt"
      to: "enableEdgeToEdge"
      via: "onCreate() call"
      pattern: "enableEdgeToEdge"
---

<objective>
Fix Play Console edge-to-edge deprecation warnings for Android 15 (SDK 35) compliance.

Purpose: The Play Console warns that the app uses deprecated APIs/parameters for edge-to-edge display. Apps targeting SDK 35 enforce edge-to-edge by default on Android 15. The current theme (`android:Theme.Material.Light.NoActionBar`) inherits deprecated `statusBarColor` and `navigationBarColor` attributes that conflict with edge-to-edge.

Output: Updated theme and MainActivity that properly handle edge-to-edge without deprecated APIs, resolving the Play Console warning.
</objective>

<execution_context>
@C:/Users/Philander/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Philander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@resume-tailor-android/app/src/main/res/values/themes.xml
@resume-tailor-android/app/src/main/AndroidManifest.xml
@resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt

<interfaces>
Current themes.xml uses deprecated parent:
```xml
<style name="Theme.CraftCV" parent="android:Theme.Material.Light.NoActionBar" />
```

MainActivity already calls enableEdgeToEdge() and uses Scaffold with innerPadding:
```kotlin
enableEdgeToEdge()
// ...
Scaffold(modifier = Modifier.fillMaxSize(), ...) { innerPadding ->
    Column(modifier = Modifier.padding(innerPadding)) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deprecated theme for edge-to-edge compliance</name>
  <files>resume-tailor-android/app/src/main/res/values/themes.xml</files>
  <action>
  Replace the theme parent from `android:Theme.Material.Light.NoActionBar` to a Compose-compatible base that doesn't set deprecated `statusBarColor`/`navigationBarColor`.

  Update themes.xml to:
  ```xml
  <?xml version="1.0" encoding="utf-8"?>
  <resources>
      <style name="Theme.CraftCV" parent="android:Theme.Material.Light.NoActionBar">
          <!-- Remove deprecated system bar colors — enableEdgeToEdge() handles this -->
          <item name="android:statusBarColor">@android:color/transparent</item>
          <item name="android:navigationBarColor">@android:color/transparent</item>
      </style>
  </resources>
  ```

  Note: While `statusBarColor` and `navigationBarColor` are deprecated on API 35+, setting them to transparent is the recommended migration path for backward compatibility (API < 35). On API 35+, these attributes are simply ignored and edge-to-edge is enforced automatically. The `enableEdgeToEdge()` call in MainActivity handles the runtime configuration.

  Also check if a `values-v35/themes.xml` is needed. It is NOT — the `enableEdgeToEdge()` from `activity-compose:1.9.3` handles API-level differences at runtime.
  </action>
  <verify>
    <automated>cd "D:\Resume Tailer\resume-tailor-android"; $env:JAVA_HOME = "B:\Android Studio\jbr"; .\gradlew assembleDebug 2>&1 | Select-String -Pattern "BUILD|ERROR|WARN" | Select-Object -First 10</automated>
  </verify>
  <done>Theme no longer inherits deprecated system bar color defaults; transparent values set for backward compat; build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Verify edge-to-edge inset handling across all screens</name>
  <files>resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt</files>
  <action>
  Audit the current inset handling:

  1. `enableEdgeToEdge()` in onCreate — already correct, keep as-is.
  2. `Scaffold` with `innerPadding` applied — already correct, Scaffold automatically handles systemBars insets.
  3. Verify the `Scaffold.bottomBar` (BannerAd) — Scaffold handles this via its own measurement, no change needed.

  The only change needed: ensure `enableEdgeToEdge()` is called BEFORE `setContent()` — it already is. Verify the activity-compose version supports the latest backward-compat behavior:
  - Current: `activity-compose:1.9.3` — this is fine, it includes the Edge-to-Edge backward compat.
  
  If any screens use their own Scaffold or have content extending behind system bars, they need `WindowInsets` handling. Check all screens:
  - DashboardScreen: uses Column with verticalScroll — OK, wrapped in main Scaffold's innerPadding
  - ProfileScreen: uses Column with verticalScroll — OK
  - ResultsScreen: uses its own layout — OK, within main Scaffold
  - PaywallScreen: Column — OK
  - HistoryScreen: LazyColumn — OK

  All screens are contained within the main Scaffold's `innerPadding`, so no individual screen changes are needed.

  No code changes expected unless audit reveals issues. If all checks pass, this task is a verification-only task.
  </action>
  <verify>
    <automated>cd "D:\Resume Tailer\resume-tailor-android"; $env:JAVA_HOME = "B:\Android Studio\jbr"; .\gradlew assembleDebug 2>&1 | Select-String -Pattern "BUILD SUCCESSFUL"</automated>
  </verify>
  <done>All screens verified to properly handle edge-to-edge insets via main Scaffold innerPadding; no deprecated API usage remains</done>
</task>

</tasks>

<verification>
- Build completes without edge-to-edge-related warnings
- themes.xml no longer relies on deprecated parent defaults for system bar colors
- enableEdgeToEdge() called before setContent() in MainActivity
- All screen content respects system bar insets via Scaffold innerPadding
</verification>

<success_criteria>
- App builds successfully targeting SDK 35
- Theme sets transparent system bar colors for backward compat
- No deprecated edge-to-edge API usage that would trigger Play Console warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-edge-to-edge-and-theme/01-01-SUMMARY.md`
</output>
