---
phase: 02-onboarding-walkthrough
plan: 01
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - resume-tailor-android/app/src/main/java/com/craftcv/app/data/preferences/UserPrefs.kt
  - resume-tailor-android/app/src/main/java/com/craftcv/app/viewmodel/MainViewModel.kt
  - resume-tailor-android/app/src/main/java/com/craftcv/app/ui/screens/OnboardingScreen.kt
  - resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]

must_haves:
  truths:
    - "First-time users see an interactive onboarding walkthrough before the dashboard"
    - "Onboarding explains key features: resume tailoring, ATS scoring, cover letters"
    - "Users can skip the walkthrough at any time"
    - "Onboarding only shows once — returning users go straight to dashboard/profile"
    - "Users can re-access the walkthrough from the dashboard"
  artifacts:
    - path: "resume-tailor-android/app/src/main/java/com/craftcv/app/ui/screens/OnboardingScreen.kt"
      provides: "Multi-step onboarding composable with pager-style navigation"
      min_lines: 80
    - path: "resume-tailor-android/app/src/main/java/com/craftcv/app/data/preferences/UserPrefs.kt"
      provides: "hasSeenOnboarding preference"
      contains: "HAS_SEEN_ONBOARDING"
  key_links:
    - from: "MainActivity.kt"
      to: "OnboardingScreen.kt"
      via: "navigation logic in CraftCVApp"
      pattern: "onboarding"
    - from: "OnboardingScreen.kt"
      to: "UserPrefs.kt"
      via: "ViewModel marking onboarding complete"
      pattern: "hasSeenOnboarding"
---

<objective>
Create an interactive onboarding walkthrough for first-time users to improve retention and feature discovery.

Purpose: Tester feedback highlighted that the app lacks a guided tour for new users, which can cause confusion and reduce retention. A step-by-step walkthrough introducing key features will help users feel comfortable and understand the app's value proposition.

Output: A multi-step onboarding screen with feature highlights, skip option, and one-time display logic.
</objective>

<execution_context>
@C:/Users/Philander/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Philander/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt
@resume-tailor-android/app/src/main/java/com/craftcv/app/data/preferences/UserPrefs.kt
@resume-tailor-android/app/src/main/java/com/craftcv/app/viewmodel/MainViewModel.kt

<interfaces>
From MainActivity.kt — navigation logic:
```kotlin
var currentScreen by remember { mutableStateOf("loading") }
LaunchedEffect(Unit) {
    currentScreen = if (viewModel.hasSeenProfile()) "dashboard" else "profile"
}
// Screens: "loading", "profile", "dashboard", "results", "history", "paywall"
```

From MainViewModel.kt:
```kotlin
suspend fun hasSeenProfile(): Boolean
fun saveProfile(profile: UserProfile)
fun skipProfile()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnboardingScreen composable</name>
  <files>resume-tailor-android/app/src/main/java/com/craftcv/app/ui/screens/OnboardingScreen.kt</files>
  <action>
  Create a new `OnboardingScreen.kt` with a multi-step pager walkthrough.

  **Design:** 3-4 pages using HorizontalPager (from `androidx.compose.foundation.pager`):

  Page 1 — "Welcome to CraftCV"
  - Icon: document/CV icon (use Icons.Outlined.Description)
  - Title: "AI-Powered Resume Tailoring"
  - Body: "Paste your resume and a job description — our AI tailors your resume for maximum impact."

  Page 2 — "ATS Score & Insights"
  - Icon: analytics icon (use Icons.Outlined.Analytics)
  - Title: "Beat the ATS"
  - Body: "Get an ATS compatibility score and detailed insights to optimize your resume for applicant tracking systems."

  Page 3 — "Cover Letters & Downloads"
  - Icon: letter icon (use Icons.Outlined.Mail)
  - Title: "Cover Letters in Seconds"
  - Body: "Generate tailored cover letters and download your polished resume as a DOCX file."

  Page 4 — "Get Started"
  - Icon: rocket icon (use Icons.Outlined.RocketLaunch)
  - Title: "Ready to Land Your Dream Job?"
  - Body: "Set up your profile and start tailoring your resume today."
  - Button: "Get Started" (calls onComplete)

  **Components:**
  - `HorizontalPager` with `PagerState`
  - Page indicator dots at bottom
  - "Skip" text button in top-right corner (calls onComplete)
  - "Next" button on pages 1-3, "Get Started" on page 4
  - Each page: Column with icon (large, tinted primary), title (headlineMedium, bold), body (bodyLarge, center-aligned)

  **Parameters:**
  ```kotlin
  @Composable
  fun OnboardingScreen(onComplete: () -> Unit)
  ```

  Use `androidx.compose.foundation.pager.HorizontalPager` and `androidx.compose.foundation.pager.rememberPagerState`. The dependency is already included via `foundation` in the Compose BOM.
  </action>
  <verify>
    <automated>cd "D:\Resume Tailer\resume-tailor-android"; $env:JAVA_HOME = "B:\Android Studio\jbr"; .\gradlew assembleDebug 2>&1 | Select-String -Pattern "BUILD SUCCESSFUL|error:"</automated>
  </verify>
  <done>OnboardingScreen.kt created with 4-page pager, skip option, page indicators, and Get Started button</done>
</task>

<task type="auto">
  <name>Task 2: Wire onboarding into navigation flow</name>
  <files>
    resume-tailor-android/app/src/main/java/com/craftcv/app/data/preferences/UserPrefs.kt,
    resume-tailor-android/app/src/main/java/com/craftcv/app/viewmodel/MainViewModel.kt,
    resume-tailor-android/app/src/main/java/com/craftcv/app/MainActivity.kt,
    resume-tailor-android/app/src/main/java/com/craftcv/app/ui/screens/DashboardScreen.kt
  </files>
  <action>
  **UserPrefs.kt:**
  1. Add key: `private val HAS_SEEN_ONBOARDING = booleanPreferencesKey("has_seen_onboarding")`
  2. Add Flow: `val hasSeenOnboarding: Flow<Boolean> = context.dataStore.data.map { it[HAS_SEEN_ONBOARDING] ?: false }`
  3. Add setter: `suspend fun setHasSeenOnboarding(value: Boolean) { context.dataStore.edit { it[HAS_SEEN_ONBOARDING] = value } }`

  **MainViewModel.kt:**
  1. Add: `suspend fun hasSeenOnboarding(): Boolean = prefs.hasSeenOnboarding.first()`
  2. Add: `fun completeOnboarding() { viewModelScope.launch { prefs.setHasSeenOnboarding(true) } }`
  3. Add: `fun resetOnboarding() { viewModelScope.launch { prefs.setHasSeenOnboarding(false) } }` (for re-access from dashboard)

  **MainActivity.kt — CraftCVApp():**
  1. Update the `LaunchedEffect(Unit)` start screen logic:
     ```kotlin
     LaunchedEffect(Unit) {
         currentScreen = when {
             !viewModel.hasSeenOnboarding() -> "onboarding"
             !viewModel.hasSeenProfile()    -> "profile"
             else                           -> "dashboard"
         }
     }
     ```
  2. Add "onboarding" screen case in the `when(currentScreen)` block:
     ```kotlin
     "onboarding" -> OnboardingScreen(
         onComplete = {
             viewModel.completeOnboarding()
             currentScreen = if (viewModel.hasSeenProfile.value) "dashboard" else "profile"
         }
     )
     ```
     Note: Use a LaunchedEffect or remember to correctly check hasSeenProfile synchronously. Alternatively, since completeOnboarding sets the flag, just navigate to "profile" — the profile screen already handles skip to dashboard.
     Simplest: `currentScreen = "profile"` after onboarding complete.

  3. Update `BackHandler` to handle "onboarding" (allow back to close app, same as profile).

  **DashboardScreen.kt:**
  Add a "Help / Tour" action (either in the header row or as a small link) that calls a new param `onReplayOnboarding: () -> Unit`:
  - In the dashboard header, add an `IconButton` with `Icons.Outlined.HelpOutline`:
    ```kotlin
    IconButton(onClick = onReplayOnboarding) {
        Icon(Icons.Outlined.HelpOutline, contentDescription = "Replay tutorial")
    }
    ```
  - In MainActivity, wire: `onReplayOnboarding = { viewModel.resetOnboarding(); currentScreen = "onboarding" }`
  </action>
  <verify>
    <automated>cd "D:\Resume Tailer\resume-tailor-android"; $env:JAVA_HOME = "B:\Android Studio\jbr"; .\gradlew assembleDebug 2>&1 | Select-String -Pattern "BUILD SUCCESSFUL|error:"</automated>
  </verify>
  <done>First launch shows onboarding → profile → dashboard; subsequent launches go to dashboard; help icon replays onboarding</done>
</task>

</tasks>

<verification>
- Fresh install (clear data): app opens to onboarding → shows 4 pages → "Get Started" → goes to profile
- Skip on page 1 → goes to profile
- After completing onboarding once, subsequent opens skip directly to dashboard
- Help/Tour icon on dashboard → returns to onboarding walkthrough
- Swiping between onboarding pages works smoothly with pager
</verification>

<success_criteria>
- Multi-step onboarding with swipeable pages and dot indicators
- Skip option available on every page
- Onboarding shown only once (persisted in DataStore)
- Re-accessible from dashboard via help icon
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-walkthrough/02-01-SUMMARY.md`
</output>
