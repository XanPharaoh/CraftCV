<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CraftCV" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Transparent system bar colors for edge-to-edge backward compat (ignored on API 35+) -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>